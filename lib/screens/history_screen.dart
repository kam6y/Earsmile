import 'package:flutter/material.dart';

/// 履歴一覧画面（Step 8 で実装）
class HistoryScreen extends StatelessWidget {
  const HistoryScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('履歴')),
    );
  }
}
